import NavContent from "./NavContent";

export default function NavDesktop() {
  return (
    <nav className="hidden lg:block border border-border py-1.5 rounded-full">
      <NavContent />
    </nav>
  );
}
